import React,{useState} from 'react'

export default function camer() {
    const [matrixm,setmatrixm]=useState([])
    const [matrixn,setmatrixn]=useState([])

    const create=(e)=>{
        for(var i=0;i<matrixm.length;i++){
            for(var j=0;j<matrixn.length;j++){
                
            }
        }
    }
  return (
    
    <div>camer</div>
  )
}
